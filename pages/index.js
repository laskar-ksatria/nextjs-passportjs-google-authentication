import React from "react";

const HomePage = () => {
  return (
    <section className="page">
      <a href="/api/google" className="login-with-google-btn">
        Sign in with Google
      </a>
    </section>
  );
};

export default HomePage;
